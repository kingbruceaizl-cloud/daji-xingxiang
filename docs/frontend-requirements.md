# 前端开发需求

## 1. 设计原则

大吉形象是工作型产品，不做营销落地页。登录后首屏应直接进入可操作的形象设计工作台。

界面风格应简洁、清晰、适合反复使用，重点突出项目、客户素材、商品选择和生成结果。

## 2. MVP 信息架构

### 第一阶段

- 无需登录即可进入项目列表、创建本地项目、体验形象大师流程。
- 登录、注册、忘记密码页面保留为后续 Supabase 上线能力，不作为 MVP 使用前置条件。

### 核心页面

- 工作台首页
- 项目列表
- 形象大师流程
- 素材与生成结果
- 后台管理

## 3. 形象大师工作流

页面：`/studio/[projectId]`

没有 Supabase 时，`/projects/new` 会创建本地项目并跳转到 `/studio/local-...`；也可以使用 `/studio/demo` 进入演示工作台。

建议采用步骤式布局：

1. 客户素材
2. 风格选择
3. 商品搭配
4. 生成图片
5. 生成视频
6. 保存与导出

### 客户素材

- 支持上传图片；未配置 Supabase 时使用本地预览兜底，上传后立即替换客户素材预览。
- 支持上传视频，第一阶段可先存储，不必完成视频理解。
- 上传控件需要在选择文件后先校验文件类型和大小，并用中文提示不支持的文件或超大文件。
- 显示素材预览、上传状态、删除按钮。

### 风格选择

- 展示后台配置的风格模板。
- 每个风格显示名称、封面图、简短描述和标签。

### 商品搭配

- 按分类选择商品：发型、妆造、衣服、裤子、鞋子、手饰/饰品等。
- 支持真实商品和素材型商品。
- 商品卡片显示图片、名称、标签、是否真实商品。
- 已选商品固定展示在右侧或底部确认栏。

### 生成图片

- 展示自动拼接后的提示词摘要。
- 支持手动补充提示词。
- 选择模型通道和模型。
- 发起生成任务后展示状态：排队中、生成中、成功、失败。
- 真实模型任务创建后自动查询 `/api/jobs/:id`，成功后展示转存到素材库的结果预览。
- 成功后展示结果图列表，支持保存到项目。
- MVP 本地模式使用演示模型通道，点击“生成形象图”即可返回演示结果并显示任务状态。

### 生成视频

- 基于选中的形象图片发起视频生成。
- 选择视频模板、脚本文案和音乐。
- 第一阶段只做短视频任务，不做复杂长视频时间轴。
- MVP 本地模式先返回演示视频封面和任务信息，真实视频地址由后续模型通道接入返回。

## 4. 后台管理页面

### 商品库

- 商品列表
- 新增/编辑商品
- 上传商品图
- 配置商品提示词描述
- 标记真实商品或素材
- 上架/下架

### 风格模板

- 风格名称
- 风格封面
- 风格标签
- 正向提示词片段
- 反向提示词片段

### 视频模板

- 模板名称
- 视频比例
- 时长
- 运镜说明
- 变装方式
- 默认脚本

### 音乐库

- 音乐名称
- 文件上传
- 情绪标签
- 适用场景

### 模型配置

- 模型通道
- 模型名称
- 能力类型
- 是否启用
- 默认参数

密钥不在前端展示明文。

## 5. 未来画布预留

MVP 不开发画布页，但数据结构和导航可以预留“创作画布”入口为禁用状态。后续再引入 React Flow，实现项目素材、生成任务和结果之间的节点关系。
